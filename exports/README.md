# Exports

Place ChatGPT-generated Export Protocol v1 JSON files in this directory. The
importer scans only `.json` files here (or a directory supplied on the command
line), and approved files can be archived after successful import.
